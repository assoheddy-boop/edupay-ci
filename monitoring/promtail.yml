server:
  http_listen_port: 9080
  grpc_listen_port: 0

positions:
  filename: /tmp/positions.yaml

clients:
  - url: http://loki:3100/loki/api/v1/push

scrape_configs:
  - job_name: educonnect-json
    static_configs:
      - targets: [localhost]
        labels:
          app: educonnect
          job: educonnect
          __path__: /var/log/educonnect/*.log
    pipeline_stages:
      - json:
          expressions:
            level: level
            message: message
            service: service
      - labels:
          level:
          service:
