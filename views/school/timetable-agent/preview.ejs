<% if (typeof _inLayout === 'undefined' || !_inLayout) { %>
<% const content = include('preview', { _inLayout: true }); %>
<%- include('../../layouts/schoolLayout', { body: content, title: title || 'Emploi du temps', user, modules, timetableAgentCss: true }) %>
<% } else { %>
<div class="page-header ta-preview-header">
  <div>
    <h1>Emploi du temps — <%= session.name %></h1>
    <p class="page-sub"><%= statusLabels[session.status] || session.status %> · <%= session.schoolYear || '' %></p>
  </div>
  <div class="btn-group">
    <button type="button" class="btn btn-outline" onclick="window.print()">Imprimer</button>
    <a href="/school/timetable-agent/<%= session.id %>?step=resultats" class="btn btn-outline">← Retour</a>
  </div>
</div>

<% if (!output) { %>
  <div class="alert alert-info">Aucun résultat généré.</div>
<% } else { %>
  <%- include('_timetableGrids', {
    timetableGrids,
    gridViewMode: gridViewMode || 'class',
    gridUid: 'preview-grid',
    showJsonExport: true,
    output,
    session,
    safeJson,
  }) %>
<% } %>
<% } %>
